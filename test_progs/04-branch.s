/* Test simple branch never-taken logic */
lda $r0, 0x00
loop:
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
bne $r0, loop
call_pal 0x555
